import { colors as c } from '../../global'
import { custom as a } from '../../global'

export default {
  container: {
    backgroundColor: a.white,
    marginBottom: -1
  },
  title: {
    color: a.red,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingTop: 12
  },
  button: {
    backgroundColor: 'red',
    width: 60,
    height: 60,
    borderRadius: 30
  },
  button_text: {
    color: a.red,
    fontSize: 12
  }
}